const mongoose = require('mongoose'); mongoose.connect('mongodb+srv://flash-space:Stirring_minds@flashspace-database.l3kzod7.mongodb.net/test').then(async () => { const db = mongoose.connection.db; const b = await db.collection('bookings').find({ affiliateId: { '': true } }).limit(5).toArray(); console.log(JSON.stringify(b.map(x => ({ type: x.type, plan: x.plan, amount: x.plan?.price })), null, 2)); process.exit(0); });
